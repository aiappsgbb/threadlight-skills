# Obsolete historical capture — NOT current evidence

`legacy/` preserves the sanitized July 2026 AGT v4/v2-manifest capture and its
August readiness reassessment byte-for-byte. These files are **not loaded by the
current runtime**, not compatible policy inputs, not current readiness scores,
and not live receipts for the canonical native Agent Hooks implementation.

Use `specs/governance-manifest.json` for the current **offline, unverified**
binding inventory. No historical deployment, image, identity, signature, or
positive badge carries forward to this runtime.
