# Local credentials & azd state — NEVER commit these.
# (Excluded when this sample was captured for public release.)
.azure/
.env
.env.*
.env.local
.threadlight/
deployment-config.json
policy-envelope.json
probe-config.json

# Python
__pycache__/
*.pyc
