# Source template only. scripts/materialize.py replaces this with Task10's
# digest-verified OPA Dockerfile and complete vendored runtime/service closure.
# Building the unmaterialized checkout must fail, not serve an ungoverned agent.
FROM scratch
COPY materialization-required /materialization-required
