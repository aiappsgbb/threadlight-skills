---
kind: sibling-skill
summary: Prove per-principal ACL enforcement over protected knowledge sources
sibling_skill: threadlight-ground
---

## Target file
N/A — this fix runs the threadlight `threadlight-ground` leg. GRD-001 is must-fix when the ground leg's ACL probes show a principal retrieving documents it is not entitled to (or the entitled/unentitled probe pair is missing) for an ACL-protected knowledge source. Remediation runs the leg (or its upstream fix) and refreshes `specs/ground-manifest.json`, whose shared envelope production-ready reads as gap evidence for `GRD-001`.

## Edit type
`sibling-skill`

## Edit recipe
1. Inspect the failing ACL rows in `specs/ground-manifest.json` (`acl_evidence[]`, `findings[].detail.leaked_document_ids`).
2. Fix the retrieval security trimming / index ACL filter so each principal only sees entitled documents, then re-capture entitled + unentitled probe evidence.
3. Re-run `threadlight-ground` to refresh the manifest.

   ```bash
   python3 skills/threadlight-ground/scripts/ground.py --project . --emit
   ```

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. GRD-001 flips from `must-fix` to `pass` once `specs/ground-manifest.json` records `GRD-001: pass` in a `complete`, fresh envelope. A `partial`, stale, or `aborted` `specs/ground-manifest.json` keeps `GRD-001` at `not-verified` (or `must-fix` on negative evidence) — an incomplete leg never inflates readiness.
