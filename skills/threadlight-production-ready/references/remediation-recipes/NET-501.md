---
kind: sibling-skill
summary: Onboard the Foundry spoke to the Citadel hub (peering + APIM Access Contract)
sibling_skill: citadel-spoke-onboarding
---

## Target file
N/A — this fix runs the awesome-gbb sibling skill `citadel-spoke-onboarding`, which performs network peering and APIM Access Contract creation in the customer's environment. There is no single repo-local file to edit; the skill emits its own scaffold + PR if the spoke needs Bicep changes.

## Edit type
`sibling-skill`

## Edit recipe
Invoke the awesome-gbb skill `citadel-spoke-onboarding` via the Copilot Skill tool. Required inputs:

```json
{
  "spoke_subscription_id": "<framing.target_subscription_id>",
  "spoke_resource_group": "<framing.target_resource_group>",
  "hub_subscription_id": "<discovered from posture context — see SKILL.md §Hub discovery>",
  "hub_apim_resource_id": "<discovered>",
  "access_contract_product": "<framing.target_posture-specific; see citadel-spoke-onboarding SKILL.md>"
}
```

If hub identity (`hub_subscription_id`, `hub_apim_resource_id`) is unknown to the agent, ask the user before invoking — never guess hub coordinates. The Citadel team owns the hub; misrouted Access Contracts are visible blast radius.

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. NET-501 should flip from `fail` to `pass` once the Access Contract reconciles in APIM. Allow ~5 minutes for propagation. If still failing:

1. Check the sibling skill's own output for partial provisioning state.
2. `az apim api list --resource-group <hub-rg> --service-name <hub-apim>` should now include the spoke's product.
3. `az network vnet peering list -g <spoke-rg> --vnet-name <spoke-vnet>` should show a peering to the hub VNet.
