---
kind: repo-edit
summary: Author backup / restore runbook documenting recovery procedures
target_file: docs/runbooks/backup-restore.md
edit_type: insert
---

## Target file
`docs/runbooks/backup-restore.md` (or `docs/backup-restore.md` if runbooks subdirectory doesn't exist). If backup documentation already exists elsewhere (e.g., `docs/backup.md`, `docs/recovery.md`), ensure it contains both backup initiation and restore steps.

## Edit type
`insert`

## Edit recipe
1. Create the directory `docs/runbooks/` if it does not exist.
2. Create `docs/runbooks/backup-restore.md` with the following sections:

   ```markdown
   # Backup and Restore Runbook
   
   ## Overview
   - **Data Scope**: <list what is backed up: databases, blobs, configuration, etc.>
   - **Backup Frequency**: <e.g., "daily", "continuous PITR">
   - **Retention Policy**: <e.g., "30 days", "90 days">
   - **RPO**: <reference from SPEC § 12>
   
   ## Backup Procedures
   - **Initiation**: <how backups are triggered: automated schedule, manual request, etc.>
   - **Location**: <backup storage account, Recovery Services Vault, or Cosmos PITR endpoint>
   - **Monitoring**: <how to verify backup completion: health check, metrics, logs>
   
   ## Restore Procedures
   - **Prerequisites**: <what must be in place before restore: network access, credentials, etc.>
   - **Step 1**: <e.g., "Authenticate to Azure">
   - **Step 2**: <e.g., "Locate the backup restore point">
   - **Step 3**: <e.g., "Trigger Cosmos PITR to target timestamp">
   - **Step 4**: <e.g., "Validate data integrity">
   - **Estimated Duration**: <RTO implication>
   
   ## Failover Procedures (if multi-region)
   - **Trigger**: <health probe failure threshold, manual trigger, etc.>
   - **Steps**: <Traffic Manager failover, update connection strings, etc.>
   
   ## Testing & Validation
   - **Restore Drill Cadence**: <e.g., "quarterly", "6-monthly">
   - **Success Criteria**: <data validation, application startup checks>
   ```

3. Fill in placeholders with deployment-specific details. Cross-reference SPEC § 12 and Bicep resource names.
4. If backup documentation already exists in a different file, create `docs/runbooks/backup-restore.md` anyway and reference the existing file, or consolidate into the new file.

## Verification
Re-run threadlight: `python3 skills/threadlight-production-ready/scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. REL-003 should flip from `fail` to `pass` when a backup/restore reference is detected in docs or Bicep.
