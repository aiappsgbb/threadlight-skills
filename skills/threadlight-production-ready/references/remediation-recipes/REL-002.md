---
kind: repo-edit
summary: Document multi-region failover plan in SPEC section 12 when RTO < 4 hours
target_file: docs/SPEC.md
edit_type: insert
---

## Target file
`docs/SPEC.md` (or the project's specification document if named differently).

## Edit type
`insert`

## Edit recipe
1. Locate section 12 "RTO / RPO Targets" (created in REL-001 or already present).
2. If RTO is explicitly stated as less than 4 hours (e.g., "1 hour", "2 hours", "3 hours", "30 minutes"):
   - Insert a new subsection titled "Multi-Region Plan" immediately after the RTO/RPO targets.
   - Add the following:

   ```markdown
   ### Multi-Region Plan
   
   - **Failover Model**: <active-passive | active-active>
   - **Primary Region**: <e.g., "eastus">
   - **Secondary Region**: <e.g., "westus" or "paired region">
   - **Data Replication**: <e.g., "Cosmos PITR", "blob geo-redundancy", "Traffic Manager failover">
   - **Failover Trigger**: <e.g., "manual", "Health Probe + automatic">
   - **RTO Validation**: <e.g., "Tested quarterly in restore drill">
   ```

3. If RTO >= 4 hours or is not specified, REL-002 defaults to `should-fix` status (not required) — no edit needed. The check will pass without a multi-region plan.
4. If RTO < 4 hours but multi-region plan already exists elsewhere in the SPEC or in Bicep, ensure the plan is referenced in SPEC § 12 explicitly (e.g., "See infra/foundry/deployments.bicep for geo-redundant Cosmos setup").

## Verification
Re-run threadlight: `python3 skills/threadlight-production-ready/scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. REL-002 should flip from `fail` to `pass` when RTO < 4h and multi-region evidence is detected (Bicep multi-region resources or SPEC multi-region block).
