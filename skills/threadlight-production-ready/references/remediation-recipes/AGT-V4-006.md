---
kind: repo-edit
summary: Pin AGT v4 composite GitHub Action via toolkit-version input
target_file: .github/workflows/agt-policy-verify.yml
edit_type: replace
---

## Target file
`.github/workflows/agt-policy-verify.yml` (or any workflow under `.github/workflows/` that uses `microsoft/agent-governance-toolkit/action`).

## Edit type
`replace` — add the `toolkit-version:` input to the AGT action step.

## Edit recipe
1. Locate the workflow file(s) under `.github/workflows/` that reference `microsoft/agent-governance-toolkit/action`.
2. For each step using that action, add the `toolkit-version:` input:

   **Before:**
   ```yaml
   - name: Run AGT Policy Verifier
     uses: microsoft/agent-governance-toolkit/action@v4
     with:
       policy-file: policy.yaml
   ```

   **After:**
   ```yaml
   - name: Run AGT Policy Verifier
     uses: microsoft/agent-governance-toolkit/action@v4
     with:
       toolkit-version: "4.1.0"
       policy-file: policy.yaml
   ```

3. Pin the `toolkit-version:` to a specific v4 release tag (e.g. `4.1.0`). Never use `latest` or `main` branch.
4. If multiple workflows use the action, add `toolkit-version:` to all of them.
5. Ensure the `uses:` pin (e.g. `@v4`) matches the major version of `toolkit-version:`.

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. AGT-V4-006 should flip from `fail` to `pass`.

Confirm the input is present in the workflow:
```bash
grep -A 5 "microsoft/agent-governance-toolkit/action" .github/workflows/*.yml | grep "toolkit-version"
```

Expected: at least one match showing `toolkit-version:` with a version string (e.g. `4.1.0`).
