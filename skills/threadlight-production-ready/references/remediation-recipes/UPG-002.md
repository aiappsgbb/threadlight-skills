---
kind: sibling-skill
summary: Address preview / runtime-policy expiry drift
sibling_skill: threadlight-upgrade
---

## Target file
N/A — this fix runs the threadlight `threadlight-upgrade` leg. UPG-002 is must-fix when a preview feature or runtime policy the pilot depends on has an expiry / deprecation date that has passed or is imminent (runtime-policy drift). Remediation runs the leg (or its upstream fix) and refreshes `specs/upgrade-manifest.json`, whose shared envelope production-ready reads as gap evidence for `UPG-002`.

## Edit type
`sibling-skill`

## Edit recipe
1. Inspect the expiry drift item in `specs/upgrade-manifest.json`; migrate off the expiring preview / bump to the GA runtime, or refresh the policy pin.
2. Re-run `threadlight-upgrade`.

   ```bash
   python3 skills/threadlight-upgrade/scripts/upgrade.py --project . --out specs/upgrade-manifest.json
   ```

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. UPG-002 flips from `must-fix` to `pass` once `specs/upgrade-manifest.json` records `UPG-002: pass` in a `complete`, fresh envelope. A `partial`, stale, or `aborted` `specs/upgrade-manifest.json` keeps `UPG-002` at `not-verified` (or `must-fix` on negative evidence) — an incomplete leg never inflates readiness.
