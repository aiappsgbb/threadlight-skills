---
kind: sibling-skill
summary: Verify knowledge-source freshness and coverage
sibling_skill: threadlight-ground
---

## Target file
N/A — this fix runs the threadlight `threadlight-ground` leg. GRD-004 is should-fix when knowledge-source freshness cannot be verified or some declared sources are uncovered / stale, or the retrieval-quality baseline reference is missing. Remediation runs the leg (or its upstream fix) and refreshes `specs/ground-manifest.json`, whose shared envelope production-ready reads as gap evidence for `GRD-004`.

## Edit type
`sibling-skill`

## Edit recipe
1. Inspect `specs/ground-manifest.json` (`findings[].detail.stale_sources` / `uncovered_sources` / `retrieval_quality_baseline`).
2. Refresh stale sources, cover the missing ones, and supply a retrieval-quality baseline reference (see threadlight-evals).
3. Re-run `threadlight-ground`.

   ```bash
   python3 skills/threadlight-ground/scripts/ground.py --project . --emit
   ```

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. GRD-004 flips to `pass`/`should-fix` once `specs/ground-manifest.json` records it in a `complete`, fresh envelope. A `partial`, stale, or `aborted` `specs/ground-manifest.json` keeps `GRD-004` at `not-verified` (or `must-fix` on negative evidence) — an incomplete leg never inflates readiness.
