---
kind: repo-edit
summary: Classify every canonical SPEC tool in the committed tool-governance contract
target_file: specs/manifest.json
edit_type: replace
---

## Target file
`specs/SPEC.md` (§ 6 tool contracts, § 8 human interaction points) and `specs/manifest.json`.

## Edit type
`replace` — update the committed tool-governance contract so it exactly matches the canonical tool surface.

## Edit recipe
1. Read every canonical tool in `specs/SPEC.md#6-tool-contracts`; do not stop at the tools the runtime already wires.
2. In `specs/manifest.json`, set `tool_governance.enabled` to `true`, keep `source.tool_contracts` pointed at `specs/SPEC.md#6-tool-contracts`, and keep `source.action_gates` pointed at `specs/SPEC.md#8-human-interaction-points`.
3. Rebuild `tool_governance.tools` so there is **exactly one** entry for every canonical SPEC § 6 tool. Each entry must declare `name`, `action_class`, `decision`, `enforcement_point`, `policy_id`, and `required_audit_fields`.
4. For every tool with `decision: "conditional"`, add a stable gate in SPEC § 8 first, then copy that exact `gate_id` into the manifest entry and include both `gate_id` and `approval_id` in `required_audit_fields`. No implicit allow: every canonical tool must be classified explicitly.
5. Regenerate any derived manifest or downstream tool-governance artifacts that the repo authors from `specs/manifest.json` so the committed contract is canonical before verification.
6. Run `python3 tests/safe_check.py --phase design` and fix every reported tool-governance gap before re-running.

## Verification
Run `python3 scripts/production_ready.py --root <REPO_ROOT> --static --pillar agent-governance` and confirm `AGT-007` is `pass`. The design gate should already be green from `python3 tests/safe_check.py --phase design`.
