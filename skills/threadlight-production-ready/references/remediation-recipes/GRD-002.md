---
kind: sibling-skill
summary: Ground answer citations in the retrieved context
sibling_skill: threadlight-ground
---

## Target file
N/A — this fix runs the threadlight `threadlight-ground` leg. GRD-002 is must-fix when answers cite sources that were not in the retrieval set (ungrounded citations) for a source that requires citations. Remediation runs the leg (or its upstream fix) and refreshes `specs/ground-manifest.json`, whose shared envelope production-ready reads as gap evidence for `GRD-002`.

## Edit type
`sibling-skill`

## Edit recipe
1. Inspect `specs/ground-manifest.json` (`citation_evidence[]`, `findings[].detail.missing_from_retrieval`).
2. Constrain the agent to cite only retrieved documents (grounded-generation / citation-enforcement), then re-capture citation evidence.
3. Re-run `threadlight-ground`.

   ```bash
   python3 skills/threadlight-ground/scripts/ground.py --project . --emit
   ```

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. GRD-002 flips to `pass` once `specs/ground-manifest.json` records `GRD-002: pass` in a `complete`, fresh envelope. A `partial`, stale, or `aborted` `specs/ground-manifest.json` keeps `GRD-002` at `not-verified` (or `must-fix` on negative evidence) — an incomplete leg never inflates readiness.
