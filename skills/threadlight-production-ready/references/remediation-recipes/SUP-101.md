---
kind: repo-edit
summary: Add a SUPPORT.md support and escalation guide at repo root
target_file: SUPPORT.md
edit_type: auto-apply
---

## Target file
`SUPPORT.md` at the repository root.

## Edit type
`auto-apply` — a remediation agent may create the file if it is missing. The production-readiness assessor itself must only read the repo and report whether the file exists.

## Edit recipe
If `SUPPORT.md` is missing, create it with this stub and replace every placeholder with customer-specific values before committing:

```markdown
# Support

## Where to file bugs

- Customer-facing incidents: <support portal, queue, or issue tracker>
- Engineering bugs: <repository issue tracker or work item board>
- Security issues: <security contact or intake process>

## Escalation path

1. Primary support owner: <team or person>
2. Engineering escalation: <team or person>
3. Executive/customer escalation: <team or person>

Include expected response times and any severity-specific routing rules.

## On-call rotation reference

- Rotation calendar: <link or system of record>
- Paging mechanism: <PagerDuty, Teams, email, or other channel>
- Backup contact: <team or person>
```

If the customer already has equivalent support documentation elsewhere, either add a short `SUPPORT.md` that links to the source of truth or file a waiver explaining why repo-root support discovery is intentionally different.

## Verification
Re-run Threadlight production readiness:

```bash
python3 scripts/production_ready.py --target-rg ${TARGET_RG} --target-sub ${TARGET_SUB}
```

SUP-101 passes when `SUPPORT.md` exists at the repository root.

## Stale-plan check (the agent MUST do this before applying)
Recompute `sha256(canonical_json(<current production-readiness-manifest.json>))` and compare against `apply_plan["manifest_sha256"]`. If they differ, the plan is stale — refuse to apply and ask the operator to re-run `--onboard` to get a fresh apply-plan. See SKILL.md "Stale-plan detection" for the full contract.
