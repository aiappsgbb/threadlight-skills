---
kind: repo-edit
summary: Add incident_owner and on-call routing declaration to SPEC § 12
target_file: docs/SPEC.md
edit_type: replace
---

## Target file
`docs/SPEC.md` (section 12 — Production Readiness)

## Edit type
`replace`

## Edit recipe
1. Locate the `## § 12 Production Readiness` section in `docs/SPEC.md`.
2. Inside this section, find or create the `incident_owner` field.
3. Replace any placeholder or empty value with a valid on-call owner:
   - Email of a monitored shared inbox (not a personal inbox)
   - Team name / alias
   - Escalation rota name

Example replacement:

```markdown
## § 12 Production Readiness

### Incident Response
- **Incident owner:** platform-oncall@company.com (or: `@SRE-L1-rota`, `oncall-team`)
- **L1 escalation:** platform-oncall@company.com
- **L2 escalation:** platform-lead@company.com
- **Runbook:** docs/runbook.md
- **Alert routing:** Teams channel #prod-incidents
```

4. Ensure the email/team/group is real and monitored 24/7, not a disabled alias.
5. If you are unsure who the incident owner should be, consult with the SRE / platform team before proceeding.

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. SRE-001 should flip from `fail` to `pass`. The check passes if SPEC § 12 contains a non-empty `incident_owner` field matching a real email or team name.
