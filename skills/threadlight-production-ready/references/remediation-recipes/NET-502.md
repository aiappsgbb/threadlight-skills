---
kind: sibling-skill
summary: Dispatch citadel-spoke-onboarding to verify Citadel spoke reachability
sibling_skill: citadel-spoke-onboarding
---

## Target file
N/A — this fix runs the awesome-gbb sibling skill `citadel-spoke-onboarding`. The skill validates the Foundry spoke connection to the Citadel hub and performs any required APIM Access Contract or spoke-onboarding changes in its own workflow.

## Edit type
`sibling-dispatch` — DO NOT edit repository files directly from the assessor. Dispatch the built sibling skill and let that skill produce its own plan, scaffold, or PR if the customer environment needs changes.

## Edit recipe
Invoke the Copilot skill `citadel-spoke-onboarding` with the current production-readiness context. Provide:

```json
{
  "spoke_subscription_id": "${framing.target_subscription_id}",
  "spoke_resource_group": "${framing.target_resource_group}",
  "hub_subscription_id": "${citadel.hub_subscription_id}",
  "hub_apim_resource_id": "${citadel.hub_apim_resource_id}",
  "access_contract_product": "${citadel.access_contract_product}"
}
```

If hub coordinates are missing, stop and have the operator obtain them from the Citadel hub owner. Do not guess hub subscription, APIM resource ID, or product names.

## Verification
Re-run Threadlight production readiness after the sibling skill completes:

```bash
python3 scripts/production_ready.py --target-rg ${TARGET_RG} --target-sub ${TARGET_SUB}
```

NET-502 should stop emitting `must-fix` once the sibling skill's Citadel reachability check is implemented and reports the spoke as connected. Until then, the fail-closed finding intentionally points back to this recipe.

## Stale-plan check (the agent MUST do this before applying)
Recompute `sha256(canonical_json(<current production-readiness-manifest.json>))` and compare against `apply_plan["manifest_sha256"]`. If they differ, the plan is stale — refuse to apply and ask the operator to re-run `--onboard` to get a fresh apply-plan. See SKILL.md "Stale-plan detection" for the full contract.
