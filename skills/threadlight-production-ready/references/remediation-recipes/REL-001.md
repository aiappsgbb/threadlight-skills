---
kind: repo-edit
summary: Add RTO and RPO targets to SPEC section 12
target_file: docs/SPEC.md
edit_type: insert
---

## Target file
`docs/SPEC.md` (or the project's specification document if named differently — search for `SPEC` or `specification` in the repo root or `docs/`).

## Edit type
`insert`

## Edit recipe
1. Locate section 12 of the SPEC (typically titled "Reliability" or "Production SLA"). If section 12 does not exist, add it after section 11.
2. Inside section 12, insert a subsection titled "RTO / RPO Targets" with this structure:

   ```markdown
   ### RTO / RPO Targets
   
   - **RTO (Recovery Time Objective)**: <target time, e.g., "4 hours" or "1 hour">
   - **RPO (Recovery Point Objective)**: <target data loss window, e.g., "1 hour" or "15 minutes">
   ```

3. Replace `<target time>` and `<target data loss window>` with values appropriate to the deployment's business requirements. Examples:
   - RTO 4h, RPO 1h: single-region acceptable, daily backups tested quarterly
   - RTO 1h, RPO 15m: requires multi-region active-passive or active-active
   - RTO 24h, RPO 24h: single-region acceptable, runbook covers restore-from-backup

4. If the SPEC already contains RTO/RPO in different language (e.g., "within 4 hours" or "no more than 1 hour"), verify it appears verbatim in section 12 under the RTO/RPO subsection. Do not paraphrase.

## Verification
Re-run threadlight: `python3 skills/threadlight-production-ready/scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. REL-001 should flip from `fail` to `pass`. The check succeeds when both `rto` and `rpo` fields are detected in SPEC § 12.
