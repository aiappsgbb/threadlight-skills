---
kind: sibling-skill
summary: Capture OBO user-scoped identity evidence for the real integration call
sibling_skill: threadlight-connect
---

## Target file
N/A — this fix runs the threadlight `threadlight-connect` leg. INT-003 tracks whether the real integration was exercised through a **user-scoped on-behalf-of (OBO) identity** — not an app-only token or an unscoped call. The connect leg records `evidence_summary.obo_present` + `obo_user_scoped` and reports INT-003 `pass` only when both hold. Remediation runs the leg (or its upstream fix) and refreshes `specs/connect-manifest.json`, whose shared envelope production-ready reads as gap evidence for `INT-003`.

## Edit type
`sibling-skill`

## Edit recipe
1. Capture a real response sample from the live endpoint **through the pilot's managed identity / OBO path** (keyless), so the OBO evidence is genuinely user-scoped. The actual on-behalf-of token exchange is owned by the `entra-agent-id` catalog skill; connect only records the *shape* of the evidence.
2. Feed that OBO evidence into `threadlight-connect` (`obo_evidence.json` with `present` + `user_scoped`) so the manifest records the identity proof alongside conformance.
3. Re-emit `specs/connect-manifest.json`.

   ```bash
   python3 skills/threadlight-connect/scripts/connect.py verify \
     --tool <tool> --sample-file real_sample.json \
     --obo-evidence-file obo_evidence.json --emit
   ```

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. INT-003 flips to `pass` once `specs/connect-manifest.json` records `INT-003: pass` (OBO present **and** user-scoped) in a `complete`, fresh envelope. OBO that is absent or present-but-not-user-scoped keeps INT-003 at `not-verified`; a `partial`, stale, or `aborted` `specs/connect-manifest.json` likewise keeps `INT-003` at `not-verified` (or `must-fix` on negative evidence) — an incomplete leg never inflates readiness.

