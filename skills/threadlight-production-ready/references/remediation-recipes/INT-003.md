---
kind: sibling-skill
summary: Capture reachability / availability evidence for the real integration
sibling_skill: threadlight-connect
---

## Target file
N/A — this fix runs the threadlight `threadlight-connect` leg. INT-003 tracks whether the real integration endpoint was actually reached and returned a healthy response during the connect leg — not just that a config points at it. Remediation runs the leg (or its upstream fix) and refreshes `specs/connect-manifest.json`, whose shared envelope production-ready reads as gap evidence for `INT-003`.

## Edit type
`sibling-skill`

## Edit recipe
1. Capture a real response sample from the live endpoint (through the pilot's managed identity / OBO path, keyless).
2. Feed that evidence into `threadlight-connect` so the manifest records reachability alongside conformance.
3. Re-emit `specs/connect-manifest.json`.

   ```bash
   python3 skills/threadlight-connect/scripts/connect.py verify --tool <tool> --sample-file real_sample.json --emit
   ```

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. INT-003 flips to `pass` (or `should-fix`) once `specs/connect-manifest.json` records it in a `complete`, fresh envelope. A `partial`, stale, or `aborted` `specs/connect-manifest.json` keeps `INT-003` at `not-verified` (or `must-fix` on negative evidence) — an incomplete leg never inflates readiness.
