---
kind: repo-edit
summary: Wire the declared runtime adapter to the committed tool-governance contract
target_file: policies/tool-governance/adapter-manifest.json
edit_type: replace
---

## Target file
`policies/tool-governance/adapter-manifest.json` (plus any generated policy artifacts it references).

## Edit type
`replace` — rewrite adapter bindings so the runtime wiring exactly matches the committed contract.

## Edit recipe
1. Read `specs/manifest.json` and `specs/foundation.md`; the adapter must preserve each governed tool's declared `enforcement_point` and match the runtime tuple (`framework`, `runtime_shape`, `protocol`).
2. In `policies/tool-governance/adapter-manifest.json`, keep `schema: threadlight.tool-governance-adapter/v1`, refresh `contract_sha256` from the canonical `tool_governance` block, and define exactly one binding per governed tool.
3. Each binding must keep the declared enforcement point, set a non-empty `adapter_id`, point `policy_artifact` at a real file, and provide `wire_signals` that resolve on disk and prove the correct adapter is live.
4. GHCP / `github-copilot-sdk` runtimes may use only `mcp-server` or `gateway`; never remap them to `agent-middleware`. Microsoft Agent Framework runtimes using `agent-middleware` must publish a real `pre-tool-policy-binding` signal. No prompt fallback or synthetic “trust me” wiring.
5. If the repo derives adapter bindings or policy artifacts from code generation, invoke `threadlight-deploy` for GHCP/MCP-or-gateway surfaces or `foundry-agt` for MAF/AGT middleware surfaces so the generated files pick up the refreshed hashes and wire signals after your edit.
6. Run `python3 tests/safe_check.py --phase predeploy` and fix every tool-governance gap before re-running.

## Verification
Run `python3 scripts/production_ready.py --root <REPO_ROOT> --static --pillar agent-governance` and confirm `AGT-008` is `pass`. The predeploy gate should already be green from `python3 tests/safe_check.py --phase predeploy`.
