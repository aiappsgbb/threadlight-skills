---
kind: sibling-skill
summary: Verify the real integration's failure-mode handling (timeout / retry / backoff)
sibling_skill: threadlight-connect
---

## Target file
N/A — this fix runs the threadlight `threadlight-connect` leg. INT-004 tracks whether the agent handles the real integration's failure modes — timeouts, throttling, transient errors — rather than assuming the mock's always-instant, always-success behaviour. Remediation runs the leg (or its upstream fix) and refreshes `specs/connect-manifest.json`, whose shared envelope production-ready reads as gap evidence for `INT-004`.

## Edit type
`sibling-skill`

## Edit recipe
1. Review the real endpoint's documented timeout / rate-limit / error contract.
2. Add timeout + retry/backoff handling in the MCP server tool wrapper and record the failure-mode coverage as connect evidence.
3. Re-emit `specs/connect-manifest.json` with `threadlight-connect`.

   ```bash
   python3 skills/threadlight-connect/scripts/connect.py verify --tool <tool> --sample-file real_sample.json --emit
   ```

## Verification
Re-run threadlight: `python3 scripts/production_ready.py --target-rg <RG> --target-sub <SUB>`. INT-004 flips to `pass`/`should-fix` once `specs/connect-manifest.json` records it in a `complete`, fresh envelope. A `partial`, stale, or `aborted` `specs/connect-manifest.json` keeps `INT-004` at `not-verified` (or `must-fix` on negative evidence) — an incomplete leg never inflates readiness.
