#!/usr/bin/env bash
# 03 — Provision VNet-joined self-hosted runners/agents (optional helper).
# Generated by threadlight-cicd v{{GENERATOR_VERSION}}. Platform team owns this infra.
set -euo pipefail

# Fill these in for your target VNet before running.
VNET_NAME="${VNET_NAME:-<target-vnet>}"
SUBNET_NAME="${SUBNET_NAME:-<runners-subnet>}"

az vmss create \
  --name "vmss-{{ENV_NAME}}-runners" \
  --resource-group "{{TARGET_RESOURCE_GROUP}}" \
  --subscription "{{TARGET_SUBSCRIPTION_ID}}" \
  --image Ubuntu2204 \
  --vnet-name "$VNET_NAME" --subnet "$SUBNET_NAME" \
  --instance-count 2 --orchestration-mode Uniform
